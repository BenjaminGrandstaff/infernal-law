apiVersion: v1
kind: Service
metadata:
  name: infernal-law
  labels:
    app.kubernetes.io/name: infernal-law
spec:
  selector:
    app.kubernetes.io/name: infernal-law
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
