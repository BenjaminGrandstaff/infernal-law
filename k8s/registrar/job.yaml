apiVersion: batch/v1
kind: Job
metadata:
  name: infernal-registrar
spec:
  backoffLimit: 3
  # Garbage-collect the Job and its Pod an hour after it finishes. Long
  # enough to read the logs after a deploy, short enough that re-applying
  # this Job on every deploy does not accumulate objects forever. Nothing is
  # lost by the deletion: what the registrar changed is recorded in the
  # kernel's own audit tables, with administrator, reason and correlation
  # ID -- the Pod log is a convenience, not the record.
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      restartPolicy: Never
      serviceAccountName: infernal-registrar
      containers:
        - name: registrar
          image: localhost/infernal-law:latest
          imagePullPolicy: IfNotPresent
          command: ["/usr/local/bin/infernal-registrar"]
          env:
            # Deliberately a different Secret from the kernel's own:
            # the registrar writes administrative tables the kernel never
            # writes, and should hold a role scoped to exactly that.
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: infernal-registrar-database
                  key: url
            - name: REGISTRAR_MANIFEST
              value: /etc/infernal-registrar/services.json
            - name: REGISTRAR_ADMINISTRATOR
              value: registrar-job
          volumeMounts:
            - name: manifest
              mountPath: /etc/infernal-registrar
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests: { cpu: 10m, memory: 16Mi }
            limits: { cpu: 250m, memory: 64Mi }
      volumes:
        # Created from registrar/services.json, for example:
        #   kubectl create configmap infernal-registrar-manifest \
        #     --from-file=services.json=registrar/services.json
        - name: manifest
          configMap:
            name: infernal-registrar-manifest
